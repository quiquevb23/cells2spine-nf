# docker/r-enrichment/Dockerfile
FROM bioconductor/bioconductor_docker:RELEASE_3_20

WORKDIR /workspace
COPY envs/renv-enrichment.lock ./renv.lock
COPY envs/activate-enrichment.R ./renv/activate.R

RUN R -e "install.packages('renv', repos = 'https://cloud.r-project.org')" && \
    R -e "renv::restore(lockfile = 'renv.lock', confirm = FALSE)"
    R -e "if (!requireNamespace('BiocManager', quietly = TRUE)) install.packages('BiocManager'); BiocManager::install('maSigPro')" && \
    R -e "install.packages('optparse', repos = 'https://cloud.r-project.org')"

# Copy scripts from bin into system PATH
COPY bin/GSEA.R bin/ORA.R bin/run_masigpro.R /usr/local/bin/
RUN chmod +x /usr/local/bin/*.R

